<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- selected -->
    <item android:drawable="@android:color/darker_gray" android:state_checkable="true" />
    <!-- pressed -->
    <item android:drawable="@android:color/darker_gray" android:state_pressed="true" />
    <!-- default -->
    <item android:drawable="@android:color/white" android:state_checkable="false" />
</selector>